
# 压缩解压缩

## tar

### 压缩文件夹为 tar.gz
```bash
tar -czvf 压缩包名.tar.gz 目录名
```

### 解压 tar.gz
```bash
tar -xzvf 压缩包名.tar.gz
```

## zip

### 压缩文件夹为 zip
```bash
zip -r 压缩包名.zip 目录名
```

### 解压 zip
```bash
unzip 压缩包名.zip
```
