# 简介
c++11引入了两个强大的关键字，可以用于在编译器进行类型推导
auto decltype
这两个关键字有不同的使用场景
auto关键字用于自动推导变量类型，其使用场景是变量初始化时，编译器可以自动推导出变量的类型，例如

# auto

```cpp
auto a = 5; // -->int
const auto b = 5; // -->const int
auto& c = a; // --> int&
auto d = b; // -->int
auto&e = b; // -->const auto&
 ```

# decltype